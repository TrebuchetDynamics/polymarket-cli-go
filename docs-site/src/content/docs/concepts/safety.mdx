---
title: Safety Model
description: Read-only by default, paper-mode locality, live gates, credential redaction — the four-layer safety guarantee in polygolem.
---

import { Aside } from "@astrojs/starlight/components";

Polygolem is **read-only by default**. Every market-data and discovery command
runs without credentials and cannot make a state-changing request. Paper mode
is **local-only**: simulated orders are stored on disk and never reach an
authenticated endpoint. Live commands require **all four gates** to pass —
`POLYMARKET_LIVE_PROFILE=on`, `live_trading_enabled: true`, `--confirm-live`,
and a successful `polygolem preflight`. Credentials are loaded only at the
moment they are needed and are **redacted** in every log, error, and JSON
output.

If any gate fails, the command aborts with a structured error and a non-zero
exit code. Polygolem will never silently downgrade a live command to paper mode
or read-only — that would hide operator intent and break automation.

<Aside type="note">
**Source of truth:** the canonical safety policy lives in
[`docs/SAFETY.md`](https://github.com/TrebuchetDynamics/polygolem/blob/main/docs/SAFETY.md).
This page is a summary; if anything here disagrees with the canonical doc,
the canonical doc wins.
</Aside>

## The four gates, in order

1. **`POLYMARKET_LIVE_PROFILE=on`** — environment opt-in.
2. **`live_trading_enabled: true`** — config-file opt-in.
3. **`--confirm-live`** — per-invocation flag.
4. **`polygolem preflight` passes** — wallet readiness, auth readiness, network
   consistency, API health.

All four. No partial credit.

## Deposit Wallet Safety (V2)

Polymarket requires deposit wallet (POLY_1271 / signature type 3) for all
trading. EOA, proxy, and Gnosis Safe are blocked by CLOB V2 for new accounts.

### Signer vs Funder Separation

The EOA remains the signing key. The deposit wallet is the funder (holds pUSD,
is the CLOB `maker`). These must never be confused:

- **EOA pUSD does NOT fund deposit-wallet orders.** CLOB reads the deposit wallet's balance.
- **Approvals must come from the deposit wallet** via relayer WALLET batch, not from the EOA.
- **Diagnostics must distinguish** `signer_eoa` from `funder/deposit_wallet` in logs.

### Builder Credential Isolation

Builder credentials authenticate with the relayer for WALLET-CREATE and WALLET batch. They are a separate auth system and must never be:
- Reused as CLOB L2 credentials
- Stored alongside CLOB API keys in the same config section
- Added to order-signing headers (orders use `builderCode`, not builder HMAC)

### Deposit-Wallet Balance Routing

When `signature_type = 3` (deposit), the CLOB balance endpoint returns the
**deposit wallet's pUSD balance**, not the EOA's. Live readiness must:
1. Check CLOB balance with `signature_type = 3`
2. Verify deposit wallet is deployed (relayer `/deployed`)
3. Verify collateral allowances are non-zero
4. Block before order submission if any check fails

### Relayer Auth vs Trading Auth

- **Relayer auth**: Builder HMAC credentials → wallet lifecycle operations
- **Trading auth**: CLOB L1/L2 credentials → order placement and balance queries
- These systems are independent. A failed relayer call must not be retried as a CLOB call.

## Related

- [`docs/SAFETY.md`](https://github.com/TrebuchetDynamics/polygolem/blob/main/docs/SAFETY.md) — canonical policy
- [Deposit Wallets & Signature Types](/concepts/deposit-wallets) — required for new accounts
- [Smart Contracts](/concepts/contracts) — factory addresses and permission model
- [Paper Trading](/guides/paper-trading) — the local-only execution surface
- [Architecture](/concepts/architecture) — where the gates live in code
